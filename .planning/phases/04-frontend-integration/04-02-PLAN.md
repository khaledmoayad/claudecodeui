---
phase: 04-frontend-integration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/project-creation-wizard/components/StepTypeSelection.tsx
  - src/components/project-creation-wizard/components/StepRemoteConfiguration.tsx
  - src/components/project-creation-wizard/components/StepRemoteDirectoryPicker.tsx
  - src/components/project-creation-wizard/ProjectCreationWizard.tsx
  - src/components/project-creation-wizard/utils/pathUtils.ts
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "Step 1 of the wizard shows a 'Remote Server' card alongside 'Existing Workspace' and 'New Workspace'"
    - "Selecting 'Remote Server' in Step 1 and clicking Next shows the SSH config form in Step 2"
    - "The SSH config form in Step 2 has fields for connection name, hostname, port, username, and private key path"
    - "Clicking 'Test Connection' calls POST /api/remote-hosts/test and shows success/failure result"
    - "After a successful connection test, clicking Next shows the remote directory browser in Step 3"
    - "The remote directory browser calls GET /api/remote-hosts/:id/browse and shows directory listing"
    - "Selecting a directory and clicking 'Add Remote Project' calls POST /api/remote-hosts/:id/add-project"
    - "After successful project creation, the wizard closes and the new project appears in the project list"
  artifacts:
    - path: "src/components/project-creation-wizard/components/StepTypeSelection.tsx"
      provides: "Third 'Remote Server' option card in wizard Step 1"
      contains: "remote"
    - path: "src/components/project-creation-wizard/components/StepRemoteConfiguration.tsx"
      provides: "SSH config form with connection test for wizard Step 2"
      contains: "testConnection"
    - path: "src/components/project-creation-wizard/components/StepRemoteDirectoryPicker.tsx"
      provides: "Remote directory browser for wizard Step 3"
      contains: "browse"
    - path: "src/components/project-creation-wizard/ProjectCreationWizard.tsx"
      provides: "Updated wizard shell with remote flow orchestration"
      contains: "StepRemoteConfiguration"
  key_links:
    - from: "StepRemoteConfiguration.tsx"
      to: "/api/remote-hosts/test"
      via: "authenticatedFetch for connection testing"
      pattern: "remoteHosts.*test"
    - from: "StepRemoteDirectoryPicker.tsx"
      to: "/api/remote-hosts/:id/browse"
      via: "authenticatedFetch for remote dir listing"
      pattern: "remoteHosts.*browse"
    - from: "ProjectCreationWizard.tsx"
      to: "/api/remote-hosts/:id/add-project"
      via: "authenticatedFetch for project registration"
      pattern: "remoteHosts.*addProject"
---

<objective>
Build the "Add Remote Project" wizard flow: add a third "Remote Server" option to Step 1, a new SSH configuration form with connection testing for Step 2 (remote), and a remote directory browser for Step 3 (remote). Wire the create flow to save the host, connect, and register the project.

Purpose: This is the primary user-facing feature of Phase 4 -- the way users discover and create remote projects.
Output: Three new/modified wizard components + updated wizard orchestrator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-integration/04-RESEARCH.md
@.planning/phases/04-frontend-integration/04-01-SUMMARY.md

@src/components/project-creation-wizard/ProjectCreationWizard.tsx
@src/components/project-creation-wizard/components/StepTypeSelection.tsx
@src/components/project-creation-wizard/components/StepConfiguration.tsx
@src/components/project-creation-wizard/components/StepReview.tsx
@src/components/project-creation-wizard/components/WizardFooter.tsx
@src/components/project-creation-wizard/components/WizardProgress.tsx
@src/components/project-creation-wizard/types.ts
@src/components/project-creation-wizard/utils/pathUtils.ts
@src/components/project-creation-wizard/data/workspaceApi.ts
@src/utils/api.js
@src/shared/view/ui/index.ts
@src/lib/utils.js

<interfaces>
<!-- Contracts from Plan 01 that this plan builds against -->

From src/utils/remote.ts (created in Plan 01):
```typescript
export function isRemoteProject(project: { name: string } | null | undefined): boolean;
export function extractHostId(project: { name: string }): string | null;
export function extractRemotePath(project: { name: string }): string | null;
```

From src/components/project-creation-wizard/types.ts (extended in Plan 01):
```typescript
export type WorkspaceType = 'existing' | 'new' | 'remote';

export type WizardFormState = {
  workspaceType: WorkspaceType;
  workspacePath: string;
  githubUrl: string;
  tokenMode: TokenMode;
  selectedGithubToken: string;
  newGithubToken: string;
  remoteHostName: string;
  remoteHostname: string;
  remotePort: number;
  remoteUsername: string;
  remotePrivateKeyPath: string;
  remoteHostId: string;
  remoteConnectionTested: boolean;
  remotePath: string;
};
```

From src/utils/api.js (extended in Plan 01):
```javascript
api.remoteHosts.list()
api.remoteHosts.create(config)
api.remoteHosts.test(config)
api.remoteHosts.connect(hostId)
api.remoteHosts.status(hostId)
api.remoteHosts.disconnect(hostId)
api.remoteHosts.browse(hostId, path)
api.remoteHosts.addProject(hostId, remotePath)
```

Server endpoints (from Plan 01):
- POST /api/remote-hosts/test -- body: { name, hostname, port, username, privateKeyPath } -- returns { success: boolean, error?: string }
- POST /api/remote-hosts -- body: { name, hostname, port, username, privateKeyPath } -- returns host object with id
- POST /api/remote-hosts/:id/connect -- returns { state, hostId }
- GET /api/remote-hosts/:id/browse?path= -- returns { path, entries: [{ name, path, type }] }
- POST /api/remote-hosts/:id/add-project -- body: { remotePath } -- returns { success, project }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Remote Server option to StepTypeSelection and create StepRemoteConfiguration</name>
  <files>
    src/components/project-creation-wizard/components/StepTypeSelection.tsx,
    src/components/project-creation-wizard/components/StepRemoteConfiguration.tsx
  </files>
  <read_first>
    src/components/project-creation-wizard/components/StepTypeSelection.tsx,
    src/components/project-creation-wizard/components/StepConfiguration.tsx,
    src/components/project-creation-wizard/types.ts,
    src/shared/view/ui/index.ts,
    src/utils/api.js,
    src/i18n/locales/en/common.json
  </read_first>
  <action>
    **StepTypeSelection.tsx changes:**
    1. Add `Server` to the lucide-react import (alongside FolderPlus, GitBranch)
    2. Change grid from `md:grid-cols-2` to `md:grid-cols-3`
    3. Add a third button card for `'remote'` after the `'new'` card, following the identical pattern:
       - onClick: `onWorkspaceTypeChange('remote')`
       - Selected style: same `border-blue-500 bg-blue-50 dark:bg-blue-900/20` pattern
       - Icon container: `bg-indigo-100 dark:bg-indigo-900/50`
       - Icon: `<Server className="h-5 w-5 text-indigo-600 dark:text-indigo-400" />`
       - Title: `t('projectWizard.step1.remote.title')`
       - Description: `t('projectWizard.step1.remote.description')`

    **StepRemoteConfiguration.tsx (new file):**
    Create a new component that renders the SSH connection form for Step 2 when `workspaceType === 'remote'`. Follow the StepConfiguration pattern exactly (same prop passing style, same form layout conventions).

    Props type `StepRemoteConfigurationProps`:
    - `remoteHostName: string` + `onRemoteHostNameChange: (v: string) => void`
    - `remoteHostname: string` + `onRemoteHostnameChange: (v: string) => void`
    - `remotePort: number` + `onRemotePortChange: (v: number) => void`
    - `remoteUsername: string` + `onRemoteUsernameChange: (v: string) => void`
    - `remotePrivateKeyPath: string` + `onRemotePrivateKeyPathChange: (v: string) => void`
    - `remoteConnectionTested: boolean`
    - `onTestConnection: () => void`
    - `isTesting: boolean`
    - `testResult: { success: boolean; error?: string } | null`

    Component structure:
    1. Title: `t('projectWizard.step2.remote.title')`
    2. Form fields using `<Input>` from shared UI, wrapped in `<div className="space-y-4">`:
       - Connection Name (remoteHostName) -- required, placeholder from i18n
       - Hostname (remoteHostname) -- required, placeholder from i18n
       - Port (remotePort) -- number input, default 22, min 1, max 65535
       - Username (remoteUsername) -- required, placeholder from i18n
       - SSH Private Key Path (remotePrivateKeyPath) -- required, placeholder from i18n, with help text
    3. Test Connection button:
       - `<Button>` from shared UI, variant="outline", onClick: onTestConnection
       - When isTesting: show `<Loader2 className="mr-2 h-4 w-4 animate-spin" />` + testing text
       - When testResult.success: show green check icon + success text
       - When testResult.error: show red X icon + error message
       - Disabled when any required field is empty or isTesting is true
    4. All labels and placeholders use `useTranslation()` with `projectWizard.step2.remote.*` keys
    5. Use `cn()` for conditional classes. Include dark mode variants for all colors.
    6. Default export for the component.
  </action>
  <verify>
    <automated>cd /home/ubuntu/claudecodeui && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Server" src/components/project-creation-wizard/components/StepTypeSelection.tsx
    - grep -q "remote" src/components/project-creation-wizard/components/StepTypeSelection.tsx
    - grep -q "md:grid-cols-3" src/components/project-creation-wizard/components/StepTypeSelection.tsx
    - grep -q "StepRemoteConfiguration" src/components/project-creation-wizard/components/StepRemoteConfiguration.tsx
    - grep -q "testConnection" src/components/project-creation-wizard/components/StepRemoteConfiguration.tsx
    - grep -q "remoteHostname" src/components/project-creation-wizard/components/StepRemoteConfiguration.tsx
    - grep -q "privateKeyPath" src/components/project-creation-wizard/components/StepRemoteConfiguration.tsx
    - grep -q "useTranslation" src/components/project-creation-wizard/components/StepRemoteConfiguration.tsx
  </acceptance_criteria>
  <done>
    - StepTypeSelection shows 3-column grid with "Remote Server" as third option using Server icon
    - Clicking "Remote Server" calls onWorkspaceTypeChange('remote')
    - StepRemoteConfiguration renders a complete SSH config form with 5 fields and Test Connection button
    - All strings use i18n translation keys
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create StepRemoteDirectoryPicker and wire remote flow in ProjectCreationWizard</name>
  <files>
    src/components/project-creation-wizard/components/StepRemoteDirectoryPicker.tsx,
    src/components/project-creation-wizard/ProjectCreationWizard.tsx,
    src/components/project-creation-wizard/utils/pathUtils.ts
  </files>
  <read_first>
    src/components/project-creation-wizard/ProjectCreationWizard.tsx,
    src/components/project-creation-wizard/components/WizardFooter.tsx,
    src/components/project-creation-wizard/components/WizardProgress.tsx,
    src/components/project-creation-wizard/data/workspaceApi.ts,
    src/components/project-creation-wizard/utils/pathUtils.ts,
    src/components/project-creation-wizard/components/StepRemoteConfiguration.tsx,
    src/utils/api.js,
    src/i18n/locales/en/common.json
  </read_first>
  <action>
    **StepRemoteDirectoryPicker.tsx (new file):**
    A component for browsing and selecting a directory on the remote host in Step 3 when `workspaceType === 'remote'`.

    Props type `StepRemoteDirectoryPickerProps`:
    - `hostId: string` -- the remote host UUID (from form state after host creation)
    - `remotePath: string` -- currently selected path
    - `onRemotePathChange: (path: string) => void`
    - `remoteHostName: string` -- display name of connection
    - `remoteHostname: string` -- hostname for display

    Internal state:
    - `currentBrowsePath: string` (starts at `/home` or remotePath or `/`)
    - `entries: Array<{ name: string; path: string; type: string }>` (directory listing)
    - `isLoading: boolean`
    - `browseError: string | null`

    Component structure:
    1. Summary section at top showing connection name + hostname (read-only info)
    2. Path breadcrumb/input showing `currentBrowsePath` with manual edit capability
    3. Directory listing panel:
       - On mount and when `currentBrowsePath` changes, call `api.remoteHosts.browse(hostId, currentBrowsePath)`, parse response, populate entries
       - Show loading spinner (`Loader2`) while fetching
       - Show error message if browse fails
       - List directory entries as clickable rows with `Folder` icon. Clicking a directory navigates into it (updates `currentBrowsePath`).
       - "Up" button (`..' or parent directory) at the top of the list using `ChevronUp` or `ArrowUp` icon, disabled at root
    4. Selected path display at bottom: show the selected `remotePath` in a highlighted box
    5. "Select this directory" button that sets `onRemotePathChange(currentBrowsePath)` -- highlights the current browse path as the selection
    6. All strings use i18n keys from `projectWizard.step3.remote.*`
    7. Use `cn()`, dark mode support, `useTranslation()`.
    8. Default export.

    **pathUtils.ts changes:**
    - The `shouldShowGithubAuthentication` function already has a guard for `workspaceType === 'new'`, so it naturally returns false for 'remote'. No change needed there.
    - The `isCloneWorkflow` function similarly returns false for 'remote'. No change needed.
    - No changes required to pathUtils.ts unless TypeScript compiler complains about exhaustive checks -- in that case, add remote handling to any switch/if statements.

    **ProjectCreationWizard.tsx changes:**
    1. Import `StepRemoteConfiguration` and `StepRemoteDirectoryPicker` at top
    2. Import `api` from `../../utils/api`
    3. Extend `initialFormState` with remote defaults:
       ```typescript
       remoteHostName: '',
       remoteHostname: '',
       remotePort: 22,
       remoteUsername: '',
       remotePrivateKeyPath: '',
       remoteHostId: '',
       remoteConnectionTested: false,
       remotePath: '',
       ```
    4. Add state for connection testing:
       ```typescript
       const [isTesting, setIsTesting] = useState(false);
       const [testResult, setTestResult] = useState<{ success: boolean; error?: string } | null>(null);
       ```
    5. Add `handleTestConnection` callback:
       - Sets isTesting=true, testResult=null
       - Reads private key: calls `api.remoteHosts.test({ name: formState.remoteHostName, hostname: formState.remoteHostname, port: formState.remotePort, username: formState.remoteUsername, privateKeyPath: formState.remotePrivateKeyPath })`
       - On success response (response.ok and body.success): set testResult={success:true}, updateField('remoteConnectionTested', true)
       - On failure: set testResult={success:false, error: body.error || 'Connection failed'}
       - Finally: setIsTesting(false)
    6. Modify `handleNext`:
       - After `if (step === 1)` block, add: For step 2 with `workspaceType === 'remote'`, validate that `remoteConnectionTested` is true. If not, set error to `t('projectWizard.errors.testConnectionFirst')` and return. Then proceed to step 3.
       - The existing step 2 validation (`workspacePath.trim()`) should only apply when `workspaceType !== 'remote'`
    7. Modify `handleCreate` for remote workflow:
       - If `formState.workspaceType === 'remote'`:
         a. Create the host: `const createRes = await api.remoteHosts.create({ name: formState.remoteHostName, hostname: formState.remoteHostname, port: formState.remotePort, username: formState.remoteUsername, privateKeyPath: formState.remotePrivateKeyPath })`
         b. Parse response, get `hostId` from response body
         c. Connect: `await api.remoteHosts.connect(hostId)` (fire-and-forget, returns 202)
         d. Add project: `const addRes = await api.remoteHosts.addProject(hostId, formState.remotePath)`
         e. Parse response, call `onProjectCreated?.(addRes.project)`
         f. Call `onClose()`
         g. Return early before the local workspace creation logic
       - Validate remotePath is not empty before creating, set error if missing
    8. In the JSX render section:
       - When `step === 2 && formState.workspaceType === 'remote'`: render `<StepRemoteConfiguration>` with all appropriate props from formState and callbacks
       - When `step === 2 && formState.workspaceType !== 'remote'`: render existing `<StepConfiguration>`
       - When `step === 3 && formState.workspaceType === 'remote'`: render `<StepRemoteDirectoryPicker>` with hostId (note: hostId may not exist yet at step 3 browse time -- see below)
       - When `step === 3 && formState.workspaceType !== 'remote'`: render existing `<StepReview>`

    **IMPORTANT design note for directory browsing without hostId:**
    At Step 3, the host has not been created/saved yet (that happens on "Create"). For the directory browser to work, we need a temporary connection. Two options:
    - Option A: Save the host when transitioning from Step 2 to Step 3 (not just on Create). This means handleNext for step 2 remote: create host, connect, store hostId, then go to step 3.
    - Option B: Use the test endpoint's successful connection info somehow.

    Use **Option A**: When user clicks Next from Step 2 (remote), the wizard:
    1. Creates the host via `POST /api/remote-hosts` -> gets hostId
    2. Connects via `POST /api/remote-hosts/:id/connect`
    3. Stores hostId in formState via `updateField('remoteHostId', hostId)`
    4. Then transitions to Step 3

    This means handleNext for step 2 remote becomes async. Wrap it: set isCreating=true, do the create+connect, then setStep(3), setIsCreating(false). On error, show error message and stay on step 2.

    If user goes Back from step 3 to step 2, the host already exists. On final Create in step 3, skip the host creation (hostId already set) and just call add-project.
  </action>
  <verify>
    <automated>cd /home/ubuntu/claudecodeui && npx tsc --noEmit 2>&1 | head -30 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "StepRemoteDirectoryPicker" src/components/project-creation-wizard/components/StepRemoteDirectoryPicker.tsx
    - grep -q "browse" src/components/project-creation-wizard/components/StepRemoteDirectoryPicker.tsx
    - grep -q "remoteHosts" src/components/project-creation-wizard/components/StepRemoteDirectoryPicker.tsx
    - grep -q "StepRemoteConfiguration" src/components/project-creation-wizard/ProjectCreationWizard.tsx
    - grep -q "StepRemoteDirectoryPicker" src/components/project-creation-wizard/ProjectCreationWizard.tsx
    - grep -q "handleTestConnection" src/components/project-creation-wizard/ProjectCreationWizard.tsx
    - grep -q "remoteHostId" src/components/project-creation-wizard/ProjectCreationWizard.tsx
    - grep -q "add-project\|addProject" src/components/project-creation-wizard/ProjectCreationWizard.tsx
  </acceptance_criteria>
  <done>
    - StepRemoteDirectoryPicker component renders a directory browser that lists remote directories via the browse API
    - ProjectCreationWizard shows StepRemoteConfiguration for step 2 when workspaceType is 'remote'
    - ProjectCreationWizard shows StepRemoteDirectoryPicker for step 3 when workspaceType is 'remote'
    - Test Connection button works: calls test API, shows result, enables/disables Next
    - Next from step 2 (remote) creates and connects host, stores hostId
    - Create from step 3 (remote) registers the project via add-project API
    - TypeScript compiles, ESLint passes
    - Full remote wizard flow: select Remote Server -> fill SSH config -> test connection -> browse dirs -> select dir -> create project
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run lint` passes
- The wizard renders without errors when workspaceType is 'remote'
- All new components use i18n translation keys (no hardcoded English strings in JSX)
- The 3-step progress bar still works correctly (no step count changes)
</verification>

<success_criteria>
- "Remote Server" option visible as third card in wizard Step 1
- Step 2 shows SSH config form with test connection when remote is selected
- Step 3 shows remote directory browser when remote is selected
- Full creation flow: test -> save host -> connect -> browse -> select dir -> register project
- All strings use i18n, all styles support dark mode
- TypeScript + ESLint pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-integration/04-02-SUMMARY.md`
</output>
