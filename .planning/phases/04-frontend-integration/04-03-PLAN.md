---
phase: 04-frontend-integration
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/sidebar/view/subcomponents/ConnectionStatusIndicator.tsx
  - src/components/sidebar/view/subcomponents/SidebarProjectItem.tsx
  - src/components/main-content/view/MainContent.tsx
autonomous: true
requirements: [CONN-03, UI-03, UI-04]

must_haves:
  truths:
    - "Remote projects in the sidebar show a Server icon instead of Folder/FolderOpen"
    - "Remote projects in the sidebar show a color-coded connection status indicator dot"
    - "The connection status indicator shows green for ready, yellow for connecting/deploying/initializing/reconnecting, gray for disconnected, red for error/failed"
    - "TaskIndicator is hidden for remote projects in the sidebar"
    - "The Tasks tab is hidden in MainContent when the selected project is remote"
    - "The connection status indicator has a tooltip showing the full state text"
  artifacts:
    - path: "src/components/sidebar/view/subcomponents/ConnectionStatusIndicator.tsx"
      provides: "Reusable connection status indicator component"
      contains: "ConnectionStatusIndicator"
      exports: ["default"]
    - path: "src/components/sidebar/view/subcomponents/SidebarProjectItem.tsx"
      provides: "Updated project item with remote icon and status indicator"
      contains: "isRemoteProject"
    - path: "src/components/main-content/view/MainContent.tsx"
      provides: "Tasks tab hidden for remote projects"
      contains: "isRemoteProject"
  key_links:
    - from: "SidebarProjectItem.tsx"
      to: "src/utils/remote.ts"
      via: "import isRemoteProject"
      pattern: "isRemoteProject"
    - from: "ConnectionStatusIndicator.tsx"
      to: "sidebar.json connectionStatus keys"
      via: "useTranslation('sidebar')"
      pattern: "connectionStatus"
    - from: "MainContent.tsx"
      to: "src/utils/remote.ts"
      via: "import isRemoteProject"
      pattern: "isRemoteProject"
---

<objective>
Add visual distinction for remote projects in the sidebar (Server icon, connection status indicator, hide TaskIndicator) and hide the Tasks tab in MainContent for remote projects.

Purpose: Users need to immediately distinguish remote projects from local ones and see their connection health at a glance. Taskmaster features are explicitly out of scope for remote projects.
Output: New ConnectionStatusIndicator component, updated SidebarProjectItem, updated MainContent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-integration/04-RESEARCH.md
@.planning/phases/04-frontend-integration/04-01-SUMMARY.md

@src/components/sidebar/view/subcomponents/SidebarProjectItem.tsx
@src/components/sidebar/view/subcomponents/TaskIndicator.tsx
@src/components/main-content/view/MainContent.tsx
@src/utils/remote.ts
@src/i18n/locales/en/sidebar.json
@src/lib/utils.js
@server/constants/remote.js

<interfaces>
<!-- Contracts from Plan 01 -->

From src/utils/remote.ts:
```typescript
export function isRemoteProject(project: { name: string } | null | undefined): boolean;
export function extractHostId(project: { name: string }): string | null;
```

From server/constants/remote.js (connection states):
```javascript
export const CONNECTION_STATES = {
  DISCONNECTED: 'disconnected',
  CONNECTING: 'connecting',
  DEPLOYING: 'deploying',
  INITIALIZING: 'initializing',
  READY: 'ready',
  RECONNECTING: 'reconnecting',
  ERROR: 'error',
  FAILED: 'failed',
};
```

From src/i18n/locales/en/sidebar.json (added in Plan 01):
```json
"connectionStatus": {
  "connected": "Connected",
  "connecting": "Connecting...",
  "deploying": "Deploying daemon...",
  "initializing": "Initializing...",
  "reconnecting": "Reconnecting...",
  "disconnected": "Disconnected",
  "error": "Connection Error",
  "failed": "Connection Failed"
}
```

From TaskIndicator.tsx (pattern to follow):
- getIndicatorConfig() maps status -> { icon, colorClassName, backgroundClassName, label, title }
- sizeClassNames and paddingClassNames for responsive sizing
- Renders as small colored dot with tooltip
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ConnectionStatusIndicator and update SidebarProjectItem</name>
  <files>
    src/components/sidebar/view/subcomponents/ConnectionStatusIndicator.tsx,
    src/components/sidebar/view/subcomponents/SidebarProjectItem.tsx
  </files>
  <read_first>
    src/components/sidebar/view/subcomponents/TaskIndicator.tsx,
    src/components/sidebar/view/subcomponents/SidebarProjectItem.tsx,
    src/utils/remote.ts,
    src/i18n/locales/en/sidebar.json,
    src/lib/utils.js,
    src/types/app.ts,
    server/constants/remote.js
  </read_first>
  <action>
    **ConnectionStatusIndicator.tsx (new file):**
    Mirror the TaskIndicator pattern closely. Create a component that displays a small color-coded dot/icon indicating the SSH connection state.

    1. Define `ConnectionState` type: `'disconnected' | 'connecting' | 'deploying' | 'initializing' | 'ready' | 'reconnecting' | 'error' | 'failed'`

    2. Define `ConnectionStatusIndicatorProps`:
       - `state: ConnectionState` (default: 'disconnected')
       - `size?: 'xs' | 'sm' | 'md'` (default: 'xs')
       - `className?: string`
       - `showLabel?: boolean` (default: false)

    3. Create `getStatusConfig(state)` function returning `{ color, darkColor, label, i18nKey }`:
       - `ready`: green -- `bg-green-500 dark:bg-green-400`, key: `connectionStatus.connected`
       - `connecting`: yellow -- `bg-yellow-500 dark:bg-yellow-400`, key: `connectionStatus.connecting`
       - `deploying`: yellow -- `bg-yellow-500 dark:bg-yellow-400`, key: `connectionStatus.deploying`
       - `initializing`: yellow -- `bg-yellow-500 dark:bg-yellow-400`, key: `connectionStatus.initializing`
       - `reconnecting`: yellow -- `bg-yellow-500 dark:bg-yellow-400`, key: `connectionStatus.reconnecting`
       - `disconnected`: gray -- `bg-gray-400 dark:bg-gray-500`, key: `connectionStatus.disconnected`
       - `error`: red -- `bg-red-500 dark:bg-red-400`, key: `connectionStatus.error`
       - `failed`: red -- `bg-red-500 dark:bg-red-400`, key: `connectionStatus.failed`

    4. Render:
       - Use `useTranslation('sidebar')` for labels
       - Simple dot: `<div>` with size classes + color classes + `rounded-full`
       - Size mapping: xs=`w-2 h-2`, sm=`w-2.5 h-2.5`, md=`w-3 h-3`
       - Wrap in a `<div>` with `title={t(config.i18nKey)}` for tooltip
       - If `showLabel`, also render `<span>` with the label text
       - `cn()` for conditional classes
       - For connecting/deploying/initializing/reconnecting states, add `animate-pulse` class to the dot for visual feedback

    5. Default export.

    **SidebarProjectItem.tsx changes:**

    1. Add imports at top:
       - `import { Server } from 'lucide-react'` (add to existing lucide import)
       - `import { isRemoteProject } from '../../../../utils/remote'`
       - `import ConnectionStatusIndicator from './ConnectionStatusIndicator'`

    2. Inside the component, after existing derived variables, add:
       ```typescript
       const isRemote = isRemoteProject(project);
       ```

    3. **Mobile view** (the `md:hidden` section):
       - Find the icon section (lines ~134-143) that renders `FolderOpen`/`Folder`
       - Wrap in a conditional: if `isRemote`, render `<Server className="h-4 w-4 text-indigo-500 dark:text-indigo-400" />` instead
       - After the project name `<h3>`, add connection status indicator for remote projects:
         ```tsx
         {isRemote && (
           <ConnectionStatusIndicator state="disconnected" size="xs" className="ml-1.5" />
         )}
         ```
       - Hide TaskIndicator for remote: change `{tasksEnabled && (` to `{tasksEnabled && !isRemote && (`

    4. **Desktop view** (the `hidden md:flex` Button section):
       - Find the icon section (lines ~284-288) that renders `FolderOpen`/`Folder`
       - Wrap in conditional: if `isRemote`, render `<Server>` icon with same sizing and indigo color
       - After the project displayName div, add connection status for remote:
         ```tsx
         {isRemote && (
           <ConnectionStatusIndicator state="disconnected" size="xs" className="ml-1.5 flex-shrink-0" />
         )}
         ```

    **NOTE on connection state:** For v1, the connection status indicator defaults to `"disconnected"`. Getting live connection state requires polling or WebSocket integration which is a v2 concern. The component is built to accept any state, but SidebarProjectItem passes a static default for now. The indicator still provides visual distinction that this is a remote project. If you can easily integrate a state fetch (e.g., call `api.remoteHosts.status(hostId)` on mount), do so. But do NOT add complex polling or WebSocket integration -- that is out of scope for this plan.

    If feasible without significant complexity: on mount (when isRemote and project is selected), do a one-time fetch of `api.remoteHosts.status(extractHostId(project))` and store the state. Use `useState` + `useEffect` with the hostId as dependency. This is a "nice to have" -- the component must work without it.
  </action>
  <verify>
    <automated>cd /home/ubuntu/claudecodeui && npx tsc --noEmit 2>&1 | head -30 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ConnectionStatusIndicator" src/components/sidebar/view/subcomponents/ConnectionStatusIndicator.tsx
    - grep -q "getStatusConfig" src/components/sidebar/view/subcomponents/ConnectionStatusIndicator.tsx
    - grep -q "animate-pulse" src/components/sidebar/view/subcomponents/ConnectionStatusIndicator.tsx
    - grep -q "connectionStatus" src/components/sidebar/view/subcomponents/ConnectionStatusIndicator.tsx
    - grep -q "isRemoteProject" src/components/sidebar/view/subcomponents/SidebarProjectItem.tsx
    - grep -q "Server" src/components/sidebar/view/subcomponents/SidebarProjectItem.tsx
    - grep -q "ConnectionStatusIndicator" src/components/sidebar/view/subcomponents/SidebarProjectItem.tsx
    - grep -q "isRemote" src/components/sidebar/view/subcomponents/SidebarProjectItem.tsx
  </acceptance_criteria>
  <done>
    - ConnectionStatusIndicator renders a color-coded dot with tooltip for each connection state
    - SidebarProjectItem shows Server icon instead of Folder for remote projects
    - SidebarProjectItem shows ConnectionStatusIndicator next to remote project name
    - TaskIndicator is hidden for remote projects in both mobile and desktop views
    - TypeScript compiles, ESLint passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Hide Tasks tab in MainContent for remote projects</name>
  <files>
    src/components/main-content/view/MainContent.tsx
  </files>
  <read_first>
    src/components/main-content/view/MainContent.tsx,
    src/utils/remote.ts,
    src/types/app.ts
  </read_first>
  <action>
    1. Add import at top of MainContent.tsx:
       ```typescript
       import { isRemoteProject } from '../../../utils/remote';
       ```

    2. Find the line (approximately line 58):
       ```typescript
       const shouldShowTasksTab = Boolean(tasksEnabled && isTaskMasterInstalled);
       ```
       Change to:
       ```typescript
       const shouldShowTasksTab = Boolean(tasksEnabled && isTaskMasterInstalled && !isRemoteProject(selectedProject));
       ```

    This single change propagates through the existing code paths:
    - `MainContentHeader` receives `shouldShowTasksTab` and conditionally renders the Tasks tab
    - `MainContentTabSwitcher` checks `shouldShowTasksTab` for tab visibility
    - `ChatInterface` receives `onShowAllTasks` which is already null when tasks are disabled
    - No other changes needed in this file -- the `shouldShowTasksTab` boolean controls everything

    3. Verify no other task-related conditional in MainContent needs the remote check. The `shouldShowTasksTab` covers all downstream consumers. If there is any `tasksEnabled &&` check that does NOT go through `shouldShowTasksTab`, add `&& !isRemoteProject(selectedProject)` there too.
  </action>
  <verify>
    <automated>cd /home/ubuntu/claudecodeui && npx tsc --noEmit 2>&1 | head -30 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "isRemoteProject" src/components/main-content/view/MainContent.tsx
    - grep -q "!isRemoteProject" src/components/main-content/view/MainContent.tsx
    - grep -q "shouldShowTasksTab.*isRemoteProject\|isRemoteProject.*shouldShowTasksTab" src/components/main-content/view/MainContent.tsx
  </acceptance_criteria>
  <done>
    - MainContent imports isRemoteProject from utils/remote
    - shouldShowTasksTab is false when the selected project is remote
    - Tasks tab, TaskIndicator, and all TaskMaster UI elements are hidden for remote projects
    - No TypeScript errors, ESLint passes
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run lint` passes
- Remote projects render with Server icon in sidebar (both mobile and desktop)
- Connection status indicator renders with correct colors
- Tasks tab is hidden when a remote project is selected
</verification>

<success_criteria>
- ConnectionStatusIndicator component exists with color-coded states and tooltip
- Remote projects show Server icon in sidebar instead of Folder
- Connection status dot visible next to remote project names
- TaskIndicator hidden for remote projects in sidebar
- Tasks tab hidden in MainContent for remote projects
- All changes support dark mode
- TypeScript + ESLint pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-integration/04-03-SUMMARY.md`
</output>
